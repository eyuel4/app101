import { AutoCloseAlertDirective } from './auto-close-alert.directive';

describe('AutoCloseAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCloseAlertDirective();
    expect(directive).toBeTruthy();
  });
});
